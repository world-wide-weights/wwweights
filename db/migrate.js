
function getCurrentState(dbConn){
	
}


function main(args: string[]){
	const sourceDB = new 
	const currentState = null
	const mode = args[0]
	let parameters = []
	if (args.length > 1){
		parameters = args.splice(1)
	}
	switch(mode){
		case 'create':
		case 'up':
		case 'down':
		case 'status':
		default:
			
	}
}

// Remove generic arguments
const args = process.args.splice(2)

