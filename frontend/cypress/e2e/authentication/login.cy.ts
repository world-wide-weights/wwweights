describe('Login', () => {
    describe('Login Flow - Email and Password', () => {
        it('should login successfull login user, redirect page and have session safed in cookie', () => {

        })

        it('should show error when register failed', () => {

        })
    })

    describe('Fields', () => {
        it('should initial hide password', () => {

        })

        it('should show password when click password icon', () => {

        })
    })
})

export { }

