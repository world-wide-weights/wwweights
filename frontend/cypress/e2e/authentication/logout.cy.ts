describe('Logout', () => {
    it('should logout (have no cookie set) when click logout', () => {

    })

    it('should redirect to login when logout on protected route', () => {

    })
})

export { }

