describe('Register', () => {
    describe('Register Flow - Email and Password', () => {
        it('should register successfull, login user, redirect page and have session safed in cookie', () => {

        })

        it('should show error when login failed', () => {

        })
    })
})

export { }

